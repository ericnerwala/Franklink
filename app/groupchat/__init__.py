"""
Group chat runtime domain.

This package owns:
- inbound routing for group chat events
- raw history recording (Supabase)
- outbound send wrappers
- feature handlers (icebreaker, invocation, etc.)
"""
